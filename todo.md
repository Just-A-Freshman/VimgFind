# 需求池

## 1. 分文件夹排除规则

用户场景：用户不同文件夹的文件夹命名非常混乱，不同文件夹需要排除的内容也不一致。需要编写非常复杂的排除规则以排除内容。

Tip：分文件夹的排除规则实际上只是用户为自己这一情况想到的一个解决方案，但实际是否存在更合适的解决方案，需要进一步探究。

复杂度：中等 / 收益：中等



## 2. 完备的索引自动更新系统

用户场景：

- 用户可能只是临时使用一下程序，但程序必须在启动后空闲时刻才能更新，导致永远无法触发自动更新；
- 用户可能将已经索引的文件夹进行移动，导致文件夹找不到，误删原有索引中的内容；
- 用户可能让程序常驻后台，并且持续对电脑进行操作。此时没有空闲时刻，但用户可能希望能立马看到索引发生对应的更新；

粗设计：

- 一个开机自启程序，以命令行形式调用VimgFind.exe，并在合适时机进行索引更新；
- 使用Watchdog追踪索引文件夹的位置。当发生移动时要能立即更新名称索引；而如果该文件夹没能落入WatchDog的跟踪范围，则要明确警告用户原有索引文件夹发生移动，并给用户留一个分支选择：
    - 让用户选择移动后的新位置，选择后抽样检测对应的图片是否存在，符合条件则提示成功；否则再次警告；
    - 以管理员形式启动，并通过操作系统中的记录追踪该文件夹的行踪；
- 单例模式：当VimgFind被用户手动启动时，及时终止命令行形态启动的VimgFind，避免冲突；

复杂度：非常高 / 收益：高



## 3. 文件夹索引图片数显示

用户场景：用户希望能知晓应用排除规则后，实际索引的图片是不是自己想要的，还需不需要增加排除条件；

Tip：实际上，该解决方案由用户提出，但这显然不是一个更好的解决方案。另外，是否所有用户都需要配置如此复杂的排除规则，我们并不知晓，需要进一步用户探讨以确知其实际收益。

复杂度：低  /  收益：低



## 4. 关闭到托盘以及冷启动

用户场景：用户使用搜图时，可能只是偶尔用一下。但又不希望占据底部任务栏的空间。如果将窗口关闭，重新启动又颇需要费一些时间，使用使用流畅度下降。

粗设计：

- 保持快速响应按钮：在程序关闭的情况下，通过开机自启预留某些进程，以保障程序的快速启动
- 托盘显示：在程序被关闭到托盘后，释放索引和模型占用内存，保持较低的内存占用；添加防抖机制，在点击显示界面后及时将索引和模型加载回来

复杂度：中等 / 收益：较高



## 5. 模型Skill

用户场景：对于普通用户而言，预留一些常用和好用的模型就足够了。但对于高级用户而言，它们可能希望接入更好的模型，但自己转化模型并能让模型被程序识别与工作非常消耗精力。

粗设计：设计一套专门给Agent的Skill，让Agent读取Skill后能分析已下载好的模型或用户提供的链接，并将模型转化成程序可识别使用的形式，并预先通过一些测试验证可用性。

复杂度：较高 / 收益：低



## 6. GPU加速

用户场景：

- 用户可能追求极致的搜索准确度，因此配置了一个非常大的第三方模型。此时使用CPU就会构成明显的性能瓶颈；
- 用户可能追求更快的索引扫描速度（但实际仅在首次扫描时有较大提速，收益较小）；

Tip：实际上，如果只是用onnxruntime-gpu来加速，对于几百MB的模型，加速效果微乎其微。这是因为，小模型下，性能瓶颈根本不在于模型本身，而是图像预处理操作。而图片预处理操作要引入GPU加速，必须引入非常笨重的第三方模块，这实际上也违背了轻量化的初心。因此这个优化是需要客观判定的。此外，使用GPU后还需要在不同设备上进行消融实验，其复杂度非常非常高，收益却并不是非常显著。

复杂度：非常高 / 收益：低

# 功能排期

## 需要调研，再决定动手

- **功能 1（分文件夹排除规则）** 和 **功能 3（显示索引图片数）**：你已经在Tip里敏锐地察觉到——“这不一定是最好的解决方案”、“不是所有用户都需要”。
- **排期建议**：**本周不要写一行代码。** 花 **1-2天** 时间，去你的用户群里（或者找几个身边的朋友），把这2个需求“人肉验证”一下。
    - 问他们：“你为什么需要按文件夹排除？是不是因为不同文件夹里的图片类别不同（比如工作素材vs私人照片）？”
    - **如果发现**：他们只是想“快速剔除不相关的截图”。那你的解决方案根本不该是“复杂的排除规则”，而应该是**“在搜索框加一个‘排除关键词’的快捷标签”**（成本极低）。
    - **结论**：等调研完，如果发现80%的人不需要，这俩功能直接砍掉；如果确实需要，再基于新方案排期（复杂度很可能从“中等”降为“低”）。



## 重点排期

- **功能 2（索引更新）**：复杂度“非常高”，但收益“高”。既然必须做，就必须动用**“灵活范围”**大法——**不要做完整版，只做“MVP最小可用版”**。
- **功能 4（托盘冷启）**：复杂度“中等”，收益“较高”。作为功能2的“伴生品”一起开发。



## 砍掉 or 无限延期

**功能 5（模型Skill）** 和 **功能 6（GPU加速）**：**复杂度极高，收益极低**。你自己在Tip里也分析了，小模型GPU加速微乎其微，且引入笨重模块违背轻量化。

# 已完成

1. 拖动图片的功能——“`不需要为了单一软件而改变操作习惯`”——非常重要
2. UNC路径问题，需要完整适配UNC路径——非常重要（已解决绝大部分UNC相关的性能问题，极大提高了UNC路径下搜索的可用性）
3. 自动的索引自动重建（在程序启动时，达到阈值后强制进行软重建；如不匹配不作破坏）



# 待讨论

## 筛选功能优化
该项功能直接服务于核心理念：极致的搜索体验；

1. 必要性讨论：现有的筛选功能，在操作流程上不太流畅。你必须先点击下拉按钮，选择控件，再点击确认，才能看到筛选结果。如果要反复操作，那么你就不得不多次触发筛选框，调整，再确定的过程。其中触发和确定这两个步骤通常而言是不必要的冗余步骤，影响了筛选的流畅性。你认为是否有必要优化筛选功能呢？讨论一下；

2. 如何设计：初步想法是，让筛选面板的宽度等于结果框的宽度，将结果框向下挤压 + 防抖 + 实时生效的机制，去掉原有的确定和取消按钮；效果类似这样：![image-20260819170923985](https://raw.githubusercontent.com/Just-A-Freshman/image-bed/main/Typora/image-20260819170923985.png)

3. 此外，由于“▼”在语义上和Combobox这种下拉框语义重叠，正常的行为下筛选框是和搜索框等宽的，但现在却超宽了。为了保证语义的清晰性，应该将图案进行修改，以和Combobox的外观进行区分；是否应该换图案，换什么图案？

4. 筛选组件应该包含哪些内容，按照什么顺序摆放？根据多个AI的观点，筛选重要性排序是：
    ```tcl
    最低相似度 > 所属文件夹 > 隐藏重复文件 > 图片类型 > 图片大小 = 修改时间
    ```

    初步设想是摆放6个组件，尽可能在结果框两行内摆完，以避免过多遮挡结果框的结果（该设想需基于2成立）；另外，在某个对角处放置“重置按钮”，用于快速重置当前的筛选条件；

5. 改造原有的筛选逻辑（需基于实际性能做判断）：将筛选逻辑从@core/search_engine.py中移出，让其统一返回所有可能结果；在：@controllers/search_controller.py的preview_smooth_result中添加一个筛选器函数，在显示时筛选；在搜索内容没有发生改变而筛选器发生改变的情况下，直接对之前保存的全部搜索结果进行过滤显示，保证效率；



